[project]
name = "a-maze-ing"
version = "0.1.0"
description = "Générateur et résolveur de labyrinthes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=5.0.4",
    "mypy>=1.14.1",
    "strenum>=0.4.15",
    "pydantic>=2.0.0",
]

[tool.mypy]
exclude = [
    '.venv/',
]
